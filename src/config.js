// src/config.js
// ─────────────────────────────────────────────
//  ⚠️  FASE 1b: Este archivo contiene la API key falsa
//  que se sube intencionalmente para luego hacer git revert
//  NO debe incluirse en .gitignore antes del push intencional
// ─────────────────────────────────────────────

const API_KEY = "mi-api-key-falsa-12345";

export default {
  apiKey: API_KEY,
};
